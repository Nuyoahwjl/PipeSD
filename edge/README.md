# Edge

Client/runner for speculative decoding experiments and result logging.

## Entry point
- `src/engine.py`

## Notes
- Sends propose/verify/exit requests to the cloud service.
- Writes experiment results under `exp/` in the working directory.
